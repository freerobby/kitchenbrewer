KitchenBrewer is a social web app for homebrewers. It is built to run on heroku.

Uses Google Apps to send email; configure like
$ heroku config:add GMAIL_SMTP_USER=username@gmail.com
$ heroku config:add GMAIL_SMTP_PASSWORD=yourpassword